import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  cameraContainer: {
    flex: 1,
  },
  buttonCamera: {
    marginBottom: 100,
    paddingBottom: 10,
  },
  cameraImg: {
    flex: 8,
  },
});
export default styles;
